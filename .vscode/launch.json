{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: find_bulges_for_vadim",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/find_bulges_for_vadim.py",
      "args": ["${workspaceFolder}/input.csv", "${workspaceFolder}/output.csv"],
      "console": "integratedTerminal"
    }
  ]
}
